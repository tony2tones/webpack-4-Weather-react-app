import React from 'react';

const WeatherType = (props) => {
    var weatherType = props.weatherType;
    {console.log(weatherType)} 
    return (
        <div> some weather text </div>
    )
    
}

export default WeatherType;